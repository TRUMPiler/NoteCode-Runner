version: '3.8'

services:
  orchestrator:
    build:
      context: ./orchestrator
    ports:
      - "5000:5000"
    volumes:
      # Mount the host's Docker socket so the container can spawn sibling containers
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped